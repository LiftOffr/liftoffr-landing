# Cross-surface consistency audit — 2026-08-11

Seven surfaces checked against one canonical fact sheet. Everything customer-facing
was verified against **live production**, not the repo.

## Canonical facts (all independently verified this session)

| Fact | Value | Verified against |
|---|---|---|
| Buy Plan | $29 one-time, `plan_MntgjXJaQnGsW`, visible | Whop API + checkout 200 |
| Cycle System | $197 one-time, `plan_WHByzwILskLsc`, visible | Whop API + checkout 200 |
| System founding | $147, `plan_3SEycpErj9Zk7`, hidden, stock 50 | Whop API + checkout 200 |
| Cycle Playbook | $497 one-time, `plan_uIpPdsPTSHdTp`, hidden | Whop API + checkout 200 |
| Course | 36 lessons + 6 overviews + 6 assessments = 48 channels | Live Discord channel tree |
| Course size | ~47,000 words (module content) | `wc -w` on `content/module*/` |
| Score indicators | **8** | `/checklist`, `/system`, homepage module 4 |
| Indicator pages | **9** at `/indicators/*` | Directory + sitemap |
| Free tier | **exactly 21** channels visible to @everyone | Computed from live permission overwrites |
| IG handle | `@torin_christianson` | Graph API, 2026-08-09 |
| Clipping | $1 / 1K views, $1,000 pool, silent clips | Campaign brief + copy v2 |

The 8-vs-9 split was the single biggest source of apparent contradiction and it
turns out to be **two different real numbers**: the Score is built from 8
indicators, and `/indicators` hosts 9 pages. Copy that says "all 9 indicator
pages" is correct; copy that says "the 9 indicators the Score is built from" was not.

---

## Fixed (live and verified in production)

| # | Surface | Was | Now |
|---|---|---|---|
| 1 | Homepage | "3 member case studies" | Removed — those three composites (Ethan/Olivia/Alex) were cut as **fabricated** on 2026-08-02 and their channels archived. The homepage kept selling them for 9 days. |
| 2 | Homepage | "a $497 value, included with every tier from day one" | "included with The Cycle System, one payment". Tier language is subscription-era, and it contradicted the paragraph directly above it. |
| 3 | `llms.txt` (live, 200) | "The Cycle Playbook ($997…)" | $497. This file is what AI search engines read — it was actively teaching them the wrong price. |
| 4 | `llms.txt` | "composite of 9 BTC cycle indicators" | 8 |
| 5 | `llms.txt` | Advertised `/api/founder-count` | Removed. That endpoint is vestigial (retired 30-spot concept, renders nowhere); llms.txt was its only public advertisement. |
| 6 | Free nurture, Day 7 | Subject: "7 days of the full system — free, no card" | The trial died 2026-08-02 and the body pitches the $29 plan. Subject replaced. |
| 7 | Free nurture, Day 1 + Day 3 | "the 9 indicators the Score is built from" (×4 spots, HTML + text) | 8 |
| 8 | `/api/cron-welcome-followups?preview=` | **Unauthenticated**, 200 on prod | Now 401. It was publishing the retired trial emails with live $99/mo + $49/mo pricing and a checkout link to a dead subscription plan. |
| 9 | `/playbook` | No fee disclosure | "$497 + Whop's 5% checkout fee ($24.85)" — `/plan` and `/system` already had theirs. |
| 10 | Discord `#how-to-use-this-course` (pinned course map) | "Fifty-five lessons, six modules" — while its own footer said "36 lessons indexed" | "Thirty-six lessons across six modules". 55 was the stale channel-map count. |
| 11 | Discord `#the-cycle-system` sales embed | "36 lessons, ~41,000 words" | ~47,000 words |
| 12 | `instagram_comment_reply.py` SCALE DM | "Three of seven top-indicators are flashing right now" | Removed. Static template, false claim (Score 34 neutral, F&G 29), and "seven" was wrong too. Points at `/cycle` now. |
| 13 | `instagram_comment_reply.py` LIFTOFFR DM | "9 on-chain indicators" | 8 |
| 14 | `instagram_comment_reply.py` header | "PLAYBOOK → ($997 offer)" | $497 |
| 15 | `CLAUDE.md` | Playbook $997; System "Whop plan TBD" | Both corrected with verified plan IDs |
| 16 | `BUSINESS_MODEL_2026-08.md` | §4/§7/§10 describe the System as unbuilt | Amendment header covering the four facts later changes made stale |

**Also: three commits were sitting unpushed** (`fde0376`, `c27e66f`, plus mine).
The Cycle System product images and the earlier lesson-count correction had never
reached production. All pushed and deployed.

---

## Flagged — needs Torin, not a mechanical fix

1. **`auto-reel` is failing.** Exit code 1; the 2026-08-09 Sunday run rendered
   **0 of 8 reels**. Every hook was rejected with "no clip with 6.0s clean" — the
   pool is 76 clips and 5 of the 8 hooks wanted "night drive POV". Either the
   clip library needs more night-drive footage or the hook-pack needs to be
   constrained to what the pool can actually satisfy. This is the top of the
   funnel and it produced nothing that week.

2. **`#trade-setups` has posted nothing since ~Aug 7.** The bot logs
   "setup still open, skipping new post" every 4h for four straight days. That is
   a paid @System channel that looks abandoned to anyone who just bought.

3. **One Discord bot token runs everything** — the whole 30-agent fleet *and* the
   MCP config, stored in plaintext. A single leak takes down every automation and
   hands over admin. Worth splitting the MCP/admin token from the fleet token.

4. **The plan-buyer email footer omits the System.** `planShell`'s standing
   "3 ways I can help" menu lists `/cycle`, `/plan`, `/playbook` — the $197 rung
   is missing from the menu shown on every plan-buyer email, including the two
   whose whole job is selling it. Copy change, so leaving it to you.

5. **`#official-links` pin is from May 12.** Still says liftoffr.com is for
   "course access, 1-on-1 booking" (course is in Discord now), and routes to no
   product page. It also lists four exchanges — non-affiliate, so no commission
   conflict, but worth a look given the `/stack` "here's what I turned down"
   position.

6. **The Day-18 re-engagement email hardcodes a market state** — "the cycle's in
   the accumulation zone". True today at Score 34; it will silently become false.

7. **`@liftoffr` vs `@torin_christianson`.** Already logged in
   `CLIPPER_CAMPAIGN_COPY_v2.md`, repeating it here so it isn't lost: the landing
   repo mentions `@liftoffr` but the connected, funnel-wired IG account is
   `@torin_christianson`.

---

## Clean bill — checked, nothing wrong

- **Whop**: all 4 live plan prices match the site exactly; all 6 subscription
  plans + both legacy founder products confirmed **hidden**; 3 archived products
  archived; all 4 checkout URLs resolve 200.
- **Role mapping**: every role ID in `whop-webhook.js` verified against live
  Discord — Plan, System, Core/Pro/Elite, both ping roles. Both System plans
  correctly map to the same `@System` addon (additive, not a tier swap).
- **GA4 revenue constants**: `{29, 197, 147, 497}` — exact match to live Whop.
- **Cron auth**: `/api/cron-welcome-followups` and `/api/cron-weekly-score` both
  401 on `?force=1`. The bypass is genuinely closed.
- **No internal docs served**: `BUSINESS_MODEL`, `CLAUDE.md`, `LIFTOFFR_MASTER_PLAN`,
  `pricing.md`, changelogs, `RATE_CARD`, `api/WHOP_SETUP_CHECKLIST` — all 404 on prod.
- **`pricing.md`** carries its SUPERSEDED banner and is not served. Correct as-is.
- **Free tier is exactly 21 channels**, computed from live permission overwrites.
- **`/system` and `/playbook` are deliberately `noindex`** per master plan §1
  (warm list only). I started to add them to the sitemap and reverted — that
  would have broken a deliberate ruling.
- **Cowen attribution intact**: enforced in code on both paths — `_source_line`
  for Discord and `_enforce_attribution` for X threads (the latter appends the
  credit even if the model omits it).
- **Hook-pack rules enforced in code**: 1-in-3 keyword CTA against the ledger,
  mandatory disclaimer with any CTA, banned-language list.
- **`ASCENSION_EVERY = 3`** in `discord_engage.py` — matches the documented
  standing experiment.
- **Auto-caption fallback is active** (sidecar-only policy reversed) and the
  publisher agrees with the captioner.
- **Shortlinks**: `/ig /yt /tt /x /start /join /founder /discord` all redirect
  correctly with UTMs intact.
- **Discord invite** `discord.gg/r4TPRbk2s3` is valid and lands on `#welcome`.
- **Discord sales embeds** (`#whats-locked-and-why`, `#the-29-buy-plan`,
  `#the-cycle-system`, `#1-on-1-playbook`) — all four prices correct, System
  correctly stated as live, disclaimers present.
- **The superseded July lineup in `#announcements`** carries an explicit
  SUPERSEDED note naming the current ladder. Handled correctly.
- **The 3 reference-sheet PDFs** promised on `/system` are present and pinned in
  `#course-resources`.
- **Clipping campaign docs** internally consistent: $1/1K, $1,000 pool (Whop
  minimum), $5 min payout, $250 max/video, silent-clip rule, thumbnail present.
- **29 of 30 LaunchAgents** at exit 0.
