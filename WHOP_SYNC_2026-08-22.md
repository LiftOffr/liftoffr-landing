# Whop ↔ Discord role sync — state and the items only Torin can do

Read from the Whop API (read-only) and the Discord API on 22 August 2026.
Whop dashboard writes are not available to this session; everything under
**"Torin only"** is staged here rather than attempted.

---

## The mapping, as it now stands

| Live product | Whop plan | Price | Visibility | Discord role | Opens |
|---|---|---|---|---|---|
| My Bear Market Buy Plan | `plan_MntgjXJaQnGsW` | $29 one-time | visible | **@Plan** `1533475043110293715` | `#plan-updates` |
| The Cycle System | `plan_WHByzwILskLsc` | $197 one-time | visible | **@System** `1535404459407048714` | full course, market-intel channels, reference sheets |
| The Cycle System — founding | `plan_3SEycpErj9Zk7` | $147 one-time | hidden | **@System** (same) | same |
| The Cycle Playbook 1:1 | `plan_uIpPdsPTSHdTp` | $497 one-time | hidden | **@Playbook** `1540544862808576050` — **NEW** | `#book-your-1-on-1` |
| Free tier | `plan_VgHcxqherM4wW` | $0 | archived | none | everything in FREE + Community + START HERE |

All four paid checkout links return HTTP 200 and point at the correct plan:
`whop.com/checkout/plan_MntgjXJaQnGsW`, `…plan_WHByzwILskLsc`, `…plan_uIpPdsPTSHdTp`,
`…plan_3SEycpErj9Zk7`. Hidden plans are still purchasable by direct link, which is how
`/playbook` and the founding link are meant to work.

## What was broken, and what changed

**The $497 Playbook granted the retired @Pro role.** `whop-webhook.js` mapped
`plan_uIpPdsPTSHdTp → "pro"` through `PLAN_TIER`. @Pro is one of the killed subscription
tiers, and it had **no view on `#book-your-1-on-1`** — so a Playbook buyer would have been
given a dead tier role and still could not open the booking channel. It has zero members,
so nobody actually hit this.

Fixed in two halves:

1. **Done, live now (Discord side):** created **@Playbook**, granted it view on
   `#book-your-1-on-1`, and *also* granted **@Pro** view there so the current unmodified
   webhook works correctly today, before any deploy.
2. **Staged, needs your deploy (repo side):** on branch `discord-launch-ready`,
   `api/whop-webhook.js` now carries `playbook` in `ADDON_ROLES` and moves
   `plan_uIpPdsPTSHdTp` out of `PLAN_TIER` into `PLAN_ADDON`. The Playbook is a separate
   1:1 product — `/playbook` says so explicitly ("this is a separate private build session,
   independent of anything else you own") — so it should not carry course access.

**The retired cardless trial no longer grants a role.** `plan_zNprCbJjAquZ6 → "pro"` was
removed from `PLAN_TIER`. The defensive `TRIAL_PLAN_IDS` guard further down, which stops a
$0 trial event being counted as revenue, was deliberately left in place.

**The buyer welcome DM was pointing at deleted things.** It named the `LIFTOFFR-HUB`
category (deleted 21 Aug), told buyers the pin in `#how-to-use-this-course` was out of date
(it is now the current map), and pointed at `#elite-qna` (renamed `#course-questions`).
Rewritten on the branch.

## The @Plan backfill question — closed

There is nothing to backfill. Whop reports **22 valid memberships**, and the only
`plan_MntgjXJaQnGsW` holders are **three test memberships, all yours**
(your own account — same email and Discord id on all three). @Plan having exactly one
member is correct, not a gap. The remaining 19 memberships are the $0 free product.

## Torin only — Whop dashboard

1. **`plan_3SEycpErj9Zk7` has `stock: 50`, `unlimited_stock: false`.** The live `/system`
   page says, in bold: *"There is no seat cap on it, because nothing here counts seats and
   I am not going to publish a number I cannot show you."* The plan config and the sales
   page contradict each other. Either set the plan to unlimited stock or change the page —
   the page is the better copy, so unlimited is probably the fix. **Nobody has bought it
   yet, so this is free to change.**
2. **Verify the webhook URL is exactly `https://liftoffr.com/api/whop-webhook`.** Confirmed
   alive: a signed-payload-less POST returns **401** (signature rejected — endpoint up and
   verifying). The three near-misses all 308 and silently break delivery:
   `http://liftoffr.com/…`, `https://www.liftoffr.com/…`, and a trailing slash.
3. **Success-redirect for `plan_MntgjXJaQnGsW` → `/welcome-plan`**, if still unset.
4. **Consider archiving the six dead subscription plans** — Core/Pro/Elite monthly and
   annual (`plan_yi7i0rC444Ssk`, `plan_kBe5idN105Ipc`, `plan_JnWiKWtwzlTVR`,
   `plan_nFxTZFYUqmMkx`, `plan_dMb9YIKbWN7ck`, `plan_b0whXHoSzqDL1`), plus
   `plan_FJ3YmVpeeF4kH` ($249 renewal on `prod_KKTF8DklVBYZx`) and
   `plan_vQax3l3E38zWz` ("Founder Lifetime — $29/mo locked in"). All hidden, none with a
   valid membership. Hidden is not the same as archived; a stale direct link still resolves.
5. **Ten of the nineteen free-tier memberships have no Discord account linked.** Not a
   defect — they signed up by email and never joined — but it is the ceiling on how many
   of them the server can ever reach.

## Torin only — deploy

Merge and deploy `discord-launch-ready` in `~/liftoffr-landing`, then:

- Remove the temporary @Pro grant on `#book-your-1-on-1` (it exists only to keep the
  pre-deploy chain working).
- **Then, and only then**, archive @Pro the same way @Core and @Elite were archived. It is
  deliberately left live tonight because the undeployed webhook still routes the $497
  product through it.

## Torin only — Vercel

`DISCORD_ZONE_ALERTS_WEBHOOK` is still unset and **there is no webhook on
`#⚡・urgent-alerts`** — I did not create one, because creating it without the Vercel
variable set produces a URL sitting unused in your integrations list. `postZoneChangeToDiscord()`
skips silently every run until both exist. Create the webhook on that channel, put the URL in
Vercel, redeploy.
