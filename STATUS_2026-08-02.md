# LIFTOFFR STATUS — 2026-08-02 (verified live ~11:30–11:45 ET)

Every claim below was checked against git, the live site, Whop/Resend/GA4/Discord/IG APIs, LaunchAgents, and logs — not the plan or session memory. UNVERIFIED items are marked with the reason.

## SURPRISES FIRST
1. **Today is Aug 2 = Day -1.** The plan's 30-day clock starts tomorrow (Aug 3). All "since Aug 3" funnel numbers are structurally zero; pre-launch baselines given instead.
2. **GitHub PAT was never rotated and is live** (old `ghp_dYP7jp3T…` token returns HTTP 200, plaintext in `liftoffr-landing/.git/config`). Worse: the full token was echoed into a session transcript during this verification. Rotate today, not Aug 5.
3. **Kalshi keys not revoked** — signed prod request returned 200 (balance $0.0053). Project is concluded; keys are live for no reason.
4. **4 of 6 repos are checked out ON plan-launch** (course, reels, video, stories) — contradicts the documented safety posture of "working trees returned to main until launch day." The two deploying repos (landing→Vercel, discord-rebuild→bot) are safely on main/master.
5. **Traffic collapsed pre-launch: 7 users last 7 days vs 23 prior (-70%)**, and the IG reel queue is empty (nothing posted today, nothing queued).
6. **Live onboarding DM bot still runs pre-pivot copy** (7-day trial, $49 Core) hourly. Consistent with the still-live trial site, but any new member gets retired pricing until launch.

## 1. LAUNCH STATE — staged, NOT live
- **plan-launch merged to main: NO, in all six repos.** Landing is on main; branch tip `4c858b7` (Aug 2 10:48) is 5 commits ahead and **1 commit ahead of origin/plan-launch (unpushed)**.
- **Site: /plan → 404. /welcome-plan → 404. /free → 307 to the OLD trial Whop checkout** (`plan_zNprCbJjAquZ6`). **/start → same trial checkout, NOT /plan.** All three pages + corrected vercel.json exist on the branch only.
- **Whop $29 plan** `plan_MntgjXJaQnGsW`: live, $29 one-time, correct product, checkout URL resolves 200 — but **visibility: hidden** (per its internal note: hidden until /plan approved). **Purchases: ZERO** (0 memberships, 0 of 28 account payments reference it; last account payment Jul 19, a $0 trial). Test promo `plantest802`: active, stock 1, **uses 0 — test buy never executed**.
- **Webhook chain: never fired, and couldn't have.** No purchase exists; the addon-role code (`applyAddonRole`, logs `[whop-webhook] addon role:`) exists only on the unmerged branch — production `whop-webhook.js` has no @Plan logic. Discord @Plan role: **0 holders**. GA4: **0 `purchase` events in 30 days**. Resend: **"LiftOffr Plan Buyers" audience does not exist**.
- **Whop post-checkout redirect: not set at plan level** (no redirect field populated; dashboard-level setting UNVERIFIED — API key lacks scope). **RESEND_PLAN_AUDIENCE_ID: NOT set** in Vercel (25 envs listed; only RESEND_API_KEY / AUDIENCE_ID / TRIAL_AUDIENCE_ID).

## 2. TORIN'S CHECKLIST
- **Reel queue: depth 0.** Drive folder holds 136 videos, all in the posted ledger (3 Jun-17 stragglers are unposted but permanently outside the poster's 20-newest scan window). **Last IG post: Aug 1 14:37Z** (V26.mov). Nothing posted Aug 2. **Buffer: PID-lock fix verified live today 13:51Z** (clean "no new videos" ticks), but last actual cross-post was **Jul 24** — the seeded ledger means Buffer only fires on NEW Drive uploads, and none have landed.
- **Key rotations:** GitHub PAT **not rotated** (old token 200; origin URL unchanged, token still embedded). GCP SA key `6a17fe18…`: file untouched since **May 8** (no new key dropped in), creation date/liveness UNVERIFIED — no gcloud installed, token-mint test permission-blocked. Kalshi `43b0b60f…`: **not revoked** (prod 200). Score: 0 of 3 done, deadline Aug 5.
- **$29 doc** (now committed on plan-launch): **T2 ledger flag UNRESOLVED** (`✅ FIRED <!-- TORIN VERIFY -->`), **§5 receipts unfilled** (`[dates + fills from ledger. TORIN: confirm…]` + `[screenshot]` placeholder), **Cowen voice-pass comment still open** (the two-sentence attribution rewrite).
- **Meta App Review: no evidence of any submission** anywhere in repos/notes/logs (only "not yet granted" code comments). Per calendar it's due Aug 4, so not late — but nothing is staged as claimed either (UNVERIFIED that staged text from Jul 26 exists as a file).

## 3. CALENDAR POSITION — Day -1, tracking AHEAD on build, behind on nothing yet
| Item (due) | Status |
|---|---|
| Buffer lock fix (Aug 3) | DONE early, verified live today |
| Refill reel queue (Aug 3) | **NOT done — queue 0, due tomorrow** |
| Strip DM promises from 7-27 batch (Aug 3) | DONE — captions clean, mp4+txt sidecar pairs ready |
| Meta App Review (Aug 4) | Not done, due in 2 days |
| 3 key rotations (Aug 5) | 0 of 3 done (see above) |
| $29 build: pages/plan/webhook/roles (Aug 5–7) | **Built early** — fully staged on branches, awaiting Torin's merge gates |
| Members migration + Discord restructure (Aug 8–9) | Not started (not due) |
Shipped but not in the plan: nothing material. Early executions (#plan-updates channel, @Plan role, gate-leaky LaunchAgent disabled, DASHBOARD_PASSWORD rotation) are all plan §0/wk-1 items pulled forward.

## 4. FUNNEL NUMBERS — window opens tomorrow; pre-launch baseline (Jul 26–Aug 2)
- **IG:** 6 posts (1 Jul 26, 4 Jul 31, 1 Aug 1), **8,893 total views**. All recycled library format. Views→site: 3 instagram/bio sessions ÷ 8,893 ≈ **0.034% CTR**.
- **Site:** **9 sessions** total (3 direct, 3 IG, 1 FB, 1 X). /plan /free /welcome-plan /start: 0 views each (404/redirects). /links: 3. **Email captures: 0** (no generate_lead/sign_up events fired).
- **Sales:** $29 count **0** (0 cold / 0 warm). System: doesn't exist yet (wk-2 build). Playbook inquiries: none in any checked channel (DMs UNVERIFIED).
- **Discord:** **70 members vs 72 baseline (-2)**. Core/Pro/Elite roles: 0/0/0 members; Founding Circle 54; @Plan 0.
- **Email:** Resend audiences — Free **15 contacts**, Trial 0, General 0. Plan-buyer sequence: doesn't exist; **no sends of any kind since Jul 12**.

## 5. GATE READOUT (Day-30, Sep 1)
- Cold $29 sales: **0 / 20 target**. Hybrid views: **0 / 500K threshold** (hybrid format doesn't exist until wk-2/3; filming unlocks ~Aug 15).
- Status: **pre-launch — on track only in the sense that nothing is late yet.** The plan itself predicts Sep 1 reads "deferred — under 500K views"; Oct 1 is the first real verdict. No asymmetric signal visible; the only directional data is bad-but-expected (0.034% CTR on recycled content vs the 0.1% target for hybrid).
- Real risk to the gate is **throughput, not conversion**: queue empty + traffic -70% + last cross-post Jul 24 means the views denominator isn't accumulating.

## 6. BLOCKED / NEEDS TORIN / NEEDS ADVISOR
**Torin, before merge (the launch gate):** T2 ledger verify + §5 receipt fill + Cowen voice-pass in PLAN_PRODUCT_DRAFT.md → plantest802 test buy → confirm `[whop-webhook] addon role:` in Vercel logs → create "LiftOffr Plan Buyers" audience + set RESEND_PLAN_AUDIENCE_ID → set Whop post-checkout redirect → /plan phone preview → unhide $29 plan → merge all six + push landing `4c858b7`.
**Torin, security:** rotate GitHub PAT **today** (token exposed in a transcript), revoke Kalshi keys, rotate GCP SA key.
**Torin, ops:** refill Drive reel queue (Aug 3 item, empty now); submit Meta App Review (Aug 4).
**Classifier-blocked (needs Torin or a Bash allow rule):** Playbook Whop plan $497→$997 repricing, hiding 2 old Founder product shells, product description fix, RESEND_TRIAL_AUDIENCE_ID removal from Vercel.
**Advisor decision not covered by plan:** none new. Watch item: whether launch waits on Meta App Review (it shouldn't per ruling 13 — keyword funnel runs on public replies + bio meanwhile).

## 7. DRIFT CHECK (plan = canon; live surfaces vs spec)
- **/start, /free, homepage, emails: still fully pre-pivot** (trial checkout, tier pricing). Expected until merge — but it means every visitor today is sold a product the plan killed.
- **Onboarding DM bot actively running trial/$49 copy** hourly (LaunchAgent loaded, exit 0). Same expected-until-merge caveat, but it's a live contradiction, not a dormant one.
- **Playbook price: live Whop plan likely still $497 vs plan's $997** — the repricing was classifier-blocked Aug 2 and no evidence it happened since (UNVERIFIED exact live price; API check of that plan not run).
- **RESEND_TRIAL_AUDIENCE_ID still set in Vercel** — trial nurture kill is code-level on the branch only.
- **#plan-updates permissions wider than spec:** besides @Plan, it allows Pro, Elite, LiftOffr Life, Founding Circle, LiftOffr roles. Spec maps it to @Plan; decide if grandfathered access is intended or tighten.
- **4 repos parked on plan-launch pre-launch** (course/reels/video/stories) — no loaded LaunchAgent deploys from them (reels Sunday plist never installed), so impact is low, but any manual `make`/deploy run now emits post-pivot content early.
- Hygiene: working Resend API key sits in a plaintext memory note (`workspace/memory/2026-05-21.md`) while `secrets/resend.env` holds a dead key; scottsdale video may carry a burned-in TRIAL end card (flagged on liftoffr-video branch, re-render warning); 3 Drive videos permanently invisible to the IG poster's 20-file window.
